#include <bits/stdc++.h>
using namespace std;

int main(){ 
    map<int, int> mp, temp; int N; cin >> N;
    for(int i = 1, temp; i <= N; i++){
        cin >> temp; mp[i] = temp;
    }
    for(int i = 1; i <= N; i++){
        for(int j = 1; j <= N; j++){
            temp = mp; temp[i] = j;
            for(int a = 1; a)
        }
    }
}