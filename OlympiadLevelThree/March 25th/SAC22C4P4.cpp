#include <bits/stdc++.h>
using namespace std;
const int MM = 1000002;
int org[MM], max[MM], min[MM];



int main(){
    int N, B, T; cin >> N >> B >> T;
    for(int i = 0; i < N;i++){
        cin >> org[MM];
    }
    
}