#include <bits/stdc++.h>
using namespace std;

int main(){
    int N, node1, node2; map<int, int> distance; map<int, vector<int>> graph;  map<int, bool> visited;
    for(int i = 0; i < 5;i++){
        cin >> N; graph.clear(); distance.clear();
        for(int j = 0; j < N;j++){
            cin >> node1 >> node2; graph[node1].push_back(node2);
        }visited[1] = true;
        for(j)

    }
}